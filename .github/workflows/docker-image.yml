name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on:
      group: Default
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
        docker-compose up -d --build